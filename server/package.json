{
  "name": "room-check-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-single.mjs",
    "test": "node build-single.mjs && node merge.test.mjs && node worker.test.mjs && node worker.single.test.mjs",
    "deploy": "wrangler deploy"
  }
}
