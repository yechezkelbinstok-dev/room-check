The current Android build, kept here so there is a plain link to download it from a phone.

Only ever one file: a new build replaces it rather than piling up beside it, because an APK is
eleven megabytes and git keeps every copy it is ever given, forever.
